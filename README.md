# hosts-switch (hs)

Простой скрипт для быстрого переключения локальных хостов при веб-разработке.

## Зачем это нужно?

Когда вы разрабатываете несколько сайтов локально, удобно добавлять их домены в `/etc/hosts` для доступа по HTTPS. Этот скрипт автоматически:
- Сканирует папку с сайтами (например, `/var/www/`)
- Добавляет все найденные домены в `/etc/hosts` (на `127.0.0.1`)
- При повторном запуске — удаляет их обратно

**Один запуск — включено, второй — выключено.**

---

## Установка

```bash
# Скачать скрипт
sudo curl -L https://github.com/fixftp/hosts-switch/raw/main/hs -o /usr/local/bin/hs

# Сделать исполняемым
sudo chmod +x /usr/local/bin/hs
```

Или скопируйте файл `hs` из этого репозитория в `/usr/local/bin/`.

---

## Использование

```bash
# Включить/выключить локальные хосты
sudo hs
```

**Пример работы:**
```
$ sudo hs
Сканирование /var/www/...
Найдено сайтов: 30 доменов (с www)
Доменов нет в /etc/hosts - добавляю...
Готово! Добавлено доменов: 30
```

Запустите ещё раз, чтобы удалить домены из `/etc/hosts`.

---

## Настройка

Откройте скрипт и измените параметры в начале файла:

```bash
# Путь к папке с сайтами
WEB_ROOT="/var/www/"

# Папки-исключения (не будут добавлены в hosts)
EXCLUDE=("logs" "backup" ".git" "cache" "tmp" "html")
```

---

## Как это работает?

1. Скрипт сканирует `/var/www/` и получает список папок
2. Исключает папки из списка `EXCLUDE`
3. Пропускает папки с пробелами в имени (невалидные домены)
4. Для каждой папки добавляет в `/etc/hosts`:
   - `домен` → `127.0.0.1`
   - `www.домен` → `127.0.0.1`
5. При повторном запуске — удаляет добавленные записи

---

## Требования

- Linux (проверено на Manjaro/Arch)
- Bash
- Права root (sudo)

---

## Лицензия

MIT

---

## Автор

fixftp

---

## Пример /etc/hosts после запуска

```
# BEGIN hs managed entries
127.0.0.1 vgif.ru www.vgif.ru tech.evr-vent.ru www.tech.evr-vent.ru evr-vent.ru www.evr-vent.ru
# END hs managed entries
```
